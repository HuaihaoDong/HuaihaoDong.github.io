# 个人站项目完整记录（交接文档）

> 用途：换设备 / 换智能体后，凭这份文档零上下文接上项目，继续完善内容。
> 最后更新：2026-09-25

---

## 一、项目概览

| 项目 | 值 |
|---|---|
| 站点主人 | 董怀昊（Tiger Captain / 老虎），北航飞行器适航技术方向研究生，烛逢户外领队 |
| 域名 | `donghuaihao.com`（阿里云注册，已实名认证） |
| GitHub 账号 | `@HuaihaoDong` |
| 站点仓库 | `HuaihaoDong/HuaihaoDong.github.io`（public，main 分支，**权威源**） |
| 线上地址 | https://www.donghuaihao.com（GitHub Pages 托管，自动 HTTPS） |
| 旧设备本地副本 | `C:\Users\admin\WorkBuddy\personal-site\` |
| 架构 | 阿里云云解析（NS 不动）→ GitHub Pages（境外免费托管）→ 自动 HTTPS |

**核心原则**：仓库是唯一权威源，本地副本只是编辑用的临时文件。所有改动最终都要 push 到 GitHub 仓库。换新设备 = 从 GitHub 拉取全部文件即可。

---

## 二、架构与预算约束

- 访客 → 阿里云云解析 → GitHub Pages（免费境外托管）→ 自动 HTTPS
- **成本**：只有域名钱（每年几块到十几块），服务器/证书/解析全部 0 元
- **预算红线**：除域名外不接受任何额外支出，所有方案必须落在免费层
- **备案**：无 ICP 备案，也不需要（服务器在境外）
- **仓库必须 public**：免费计划下 `username.github.io` 仓库设 private 会导致 Pages 下线（404）

### 阿里云解析记录（已配好，别动 NS）

| 主机记录 | 类型 | 记录值 |
|---|---|---|
| `www` | CNAME | `huaihaodong.github.io` |
| `@` | A | `185.199.108.153` / `109.153` / `110.153` / `111.153`（4 条） |

---

## 三、文件清单与作用

仓库根目录下的文件（**权威源**）：

### 页面（HTML）
| 文件 | 作用 |
|---|---|
| `index.html` | 首页：星空背景 + 粒子大字「Tiger Captain」+ 鼠标交互 + 导航栏 + 宠物老虎 |
| `about.html` | 关于我：五分区（何方神圣/烛逢老虎领队/兴趣爱好/科研/有趣的人）+ 吸顶目录 scroll-spy |
| `story.html` | 我和烛逢的故事：活动列表页（数据驱动，读 activities.json） |
| `detail.html` | 活动详情页（读 URL 参数 id，展示单次活动的心得/照片/视频） |
| `group.html` | 我的小组：烛逢小程序（报名享折扣）+ 微信群「快乐老家」+ 群公告 |
| `reviews.html` | 小伙伴对老虎的评价：评价截图列表（读 reviews.json） |
| `tools.html` | 工具箱：自研工具（Wang Nan 3 个）/ 常用在线工具 16 个 / 有趣网站 3 个 |

### 样式与脚本
| 文件 | 作用 |
|---|---|
| `common.css` | 全站共享样式：配色 token、导航栏、老虎、卡片/按钮/标签、响应式断点 |
| `nav.js` | 导航栏交互：滚动毛玻璃、下拉菜单、汉堡菜单、手风琴、当前页高亮 |
| `tiger.js` | 宠物老虎彩蛋：精灵图逐帧动画 + 点哪跑哪 + 待机循环 + 方向翻转 + 点击互动 |

### 数据文件（重点，以后填内容主要改这里）
| 文件 | 作用 |
|---|---|
| `activities.json` | 活动数据数组（story 列表页 + detail 详情页共用） |
| `reviews.json` | 评价截图路径数组 |

### 图片资源
| 文件 | 说明 |
|---|---|
| `tiger-idle.png` / `tiger-run8.png` / `tiger-sit.png` / `tiger-sleep.png` / `tiger-play.png` | 老虎精灵图（横向帧条，已去背、缩到 128px 帧） |
| `tiger-run.png` | 6 帧跑步备用版（当前用 run8 的 8 帧） |
| `tiger.jpg` | 旧的真实幼虎照片，**已不用**，可删 |
| `group-qr.jpg` | 小组小程序二维码（竖版，缩到 320px 宽） |
| `reviews/review-1.jpg` ~ `review-4.jpg` | 评价截图（已统一到 1103×666） |

### 其他
| 文件 | 说明 |
|---|---|
| `CNAME` | 内容 `www.donghuaihao.com`，别动 |
| `README.md` | 仓库说明 |
| `style.css` | **旧文件，已不被引用，可删** |

---

## 四、设计语言与配色 token

| token | 色值 | 用途 |
|---|---|---|
| 背景 | `#0B1120` | 深海军蓝底 |
| 面板 | `#141d33` / `#1a2540` | 卡片/面板 |
| 描边 | `#2a3a55` | 细描边 |
| 强调色 | `#c9a26b` | 金棕，按钮/链接/标签 |
| 主文字 | `#e9e6e0` | 标题 |
| 次文字 | `#a5acbd` | 正文/简介 |
| 淡文字 | `#6b7488` | 占位/脚注 |

- **标题字体**：等宽（`Consolas, "Courier New", "PingFang SC", monospace`）+ 大字距，营造「点阵」气质
- **正文字体**：system-ui 无衬线（禁止全站等宽）
- **卡片 hover**：轻微上浮 `translateY(-3px)` + 描边变金棕，不要阴影轰炸和炫光

### 首页视觉（已定稿）
- 背景：深蓝黑渐变 + canvas 银河带 + 动态「星座网」（星星漂移 + 近距离连线 + 闪烁）
- 主视觉：canvas 粒子组成的 `Tiger Captain`，粒子色 **暖橙 `#e0a878` → 冷紫 `#8a7ab0`** 从左到右渐变
- 鼠标移到文字上：粒子被拨开散掉，移开回弹
- 下方：可点击的「董怀昊」（跳 about.html#who）+ 「北京航空航天大学 · 飞行器适航技术」+ 「烛逢领队 · 老虎」标签（跳 about.html#leader）+ GitHub 按钮

---

## 五、数据文件格式（填内容核心）

### activities.json —— 活动数据
数组，每个活动一个对象，字段如下：

```json
{
  "id": "20240912-shuiquan",          // 唯一编号（随意起，用于 detail.html?id=xxx）
  "title": "水泉沟徒步",               // 活动标题
  "date": "2024-09-12",                // 日期（YYYY-MM-DD，用于排序）
  "location": "北京·延庆水泉沟",        // 地理位置
  "weather": "晴 18°C",                // 天气
  "cover": "photos/shuiquan-cover.jpg", // 封面图路径（列表页+详情页顶部）
  "summary": "一句话摘要",              // 列表页显示的摘要
  "note": "完整心得，可多行（用 \\n 分段）", // 详情页完整心得
  "pinned": false,                     // true = 置顶（排最前 + 📌 标记）
  "photos": ["photos/shuiquan-1.jpg"], // 照片数组
  "videos": [{"src": "videos/shuiquan.mp4", "poster": "可选封面"}]  // 视频数组
}
```

**排序规则**：置顶的（pinned=true）排最前，其余按 date 倒序（最新靠上）。页面自动处理，无需改 HTML。

### reviews.json —— 评价截图
字符串数组，每项是评价图片路径：
```json
["reviews/review-1.jpg", "reviews/review-2.jpg", "..."]
```

---

## 六、关键操作指南（新设备接手续做）

### 1. 拉取项目到新设备
从 GitHub 仓库 `HuaihaoDong/HuaihaoDong.github.io` 下载/克隆全部文件。仓库是权威源。

### 2. 上传文件到 GitHub（核心操作）
用现成脚本 `gh.py`（路径见下文「技术环境」）：
```bash
# 上传文本文件（自动处理 sha）
python gh.py write HuaihaoDong/HuaihaoDong.github.io 路径/文件名 --file 本地文件 -m "提交说明"

# 上传二进制（图片等），用 base64 + REST API（见仓库里 upload_*.py 脚本的写法）
```

### 3. 加一个活动（story）
1. 把封面图、照片放到仓库（如 `photos/` 目录，先上传图片）
2. 在 `activities.json` 数组里加一个对象（按上面格式）
3. 上传 `activities.json`
4. 视频：压缩后放仓库用 `<video>` 标签，或传 B 站用 iframe 嵌入（见「视频方案」）

### 4. 加一条评价（reviews）
1. 新评价截图放进 `input_reviews/` 目录
2. 运行 `normalize_reviews.py`（自动统一到 1103×666 输出到 reviews/）
3. 上传新生成的 `review-N.jpg`
4. 在 `reviews.json` 里加路径
5. 上传 `reviews.json`

**评价图标准化规则（用户明确要求，务必遵守）**：
- 标准尺寸 **1103 × 666**（源自桌面「评价.jpg」）
- 左右（横向）：定位到绝对值 1103，不足左右补白、超出左右对称截取
- 上下（纵向）：统一到 666，不足上下补白、超出上下对称截取
- **两边要同时截，绝不只截一边**

### 5. 改老虎动画节奏（tiger.js）
`tiger.js` 里的 `ACTIONS` 对象，每套动作的 `dur` 是循环秒数（越大越慢）：
```js
run: { img: 'tiger-run8.png', frames: 8, dur: 1.0 },  // 改 dur 调跑动快慢
```
移动速度在 `loop` 函数里：`Math.min(dist * 0.05, 9)`，两个数字越小跑得越慢。

### 6. 改配色
所有页面顶部 `<style>` 里的 `:root` 变量，或 `common.css` 的 token。改了色值上传即生效。

---

## 七、视频方案（重要）

GitHub Pages 免费托管，视频三种放法：

| 方案 | 做法 | 适合 | 缺点 |
|---|---|---|---|
| **A. 压缩后放仓库（推荐）** | ffmpeg 压到 720p，几十秒压到 3~10MB，`<video>` 标签播 | 短视频 <5 分钟 | 仓库变大 |
| **B. B 站上传 + iframe 嵌入** | 传 B 站，网页嵌 B 站播放器 | 长视频/大视频 | 有 logo、要过审 |
| **C. 图床/CDN** | 复杂 | 不推荐 | — |

旧设备上 ffmpeg 未安装，需要时可装（或用现成压缩工具）。照片直接用 PIL 压缩（Pillow 已装）。

---

## 八、素材与资源位置（旧设备桌面）

用户的活动照片/资料都在旧设备，**尚未全部上传到仓库**，新设备如需用要从旧设备拷贝：

| 位置 | 内容 |
|---|---|
| `桌面\董怀昊\烛逢\` | 领队培训、领队带队、`26.08.30金海湖照片` 等 |
| `桌面\老虎精灵图\` | 老虎精灵图素材包（含 `frames/` 单帧、`run8_strip.png`、`README.md`） |
| `桌面\评价.jpg`、`评价2.png`、`评价3.png`、`评价4.png` | 评价截图原始素材 |
| `桌面\小组二维码.jpg` | 小程序二维码（竖版 981×1471） |
| `桌面\群公告1.0.md`、`老虎朋友们的"快乐老家".md` | 微信群简介 + 完整公告 |
| `桌面\donghuaihao二维码.png` | 已生成的网站二维码（中心老虎 logo，H 纠错，永久） |
| `桌面\个人站与后端架构备忘.md` | 早期架构备忘 |

---

## 九、技术环境与工具（旧设备）

| 项 | 路径/值 |
|---|---|
| Python | `C:/Users/admin/.workbuddy/binaries/python/versions/3.13.12/python.exe`（一律用绝对路径调用） |
| GitHub 操作脚本 | `C:/Users/admin/.workbuddy/skills/github-api/gh.py`（命令：me/repos/ls/read/write/delete/branches 等） |
| 凭据 | 环境变量 `GITHUB_PAT`（2026-12-16 到期），文档中不留明文 |
| 已装库 | Pillow（图片处理）、qrcode（二维码生成） |

### 坑（务必注意）
1. **Windows 下 Python 输出中文乱码**：运行 Python 时加环境变量 `PYTHONIOENCODING=utf-8`
2. **GitHub Pages 部署延迟**：上传后 1~2 分钟才生效，且浏览器有缓存，要 `Ctrl+F5` 强刷
3. **`username.github.io` 必须 public**（免费计划），不能设 private
4. **gh.py 没有建仓库/开 Pages 命令**，需用 REST API
5. **Windows Git Bash 缺 head/tail/tr 等命令**，python 用绝对路径

---

## 十、待办清单（后续要做的）

1. **填活动数据**：`activities.json` 现在还是 2 条占位示例，需要换成真实活动（水泉沟徒步、金海湖、飞拉达等）
2. **小程序宣传文案**：group.html 里现在是总结版占位，可再精确
3. **微信群二维码**：group.html 里群二维码还是占位（`qr-placeholder`），等用户提供二维码图
4. **更多评价截图**：reviews 现有 4 条，用户说共 35 条+，后续陆续加
5. **关于我页占位内容**：
   - #who 简介：「这里得好好写一下，先空着叭」
   - #leader 照片条：5 张「照片待补充」
   - #hobby 成绩牌：已填百米/接力/足球/徒步/滑雪/攀岩/萨克斯
   - #research：论文/专利/其他成果占位
   - #friends：Wang Nan（大佬舍友）+ 2 个占位
6. **老虎精灵图精修**：当前是 AI 生成的精灵图，可用（可选的后续优化）
7. **清理旧文件**：`style.css`、`tiger.jpg` 已不用，可删
8. **备忘里的老待办**：确认根域 A 记录生效、Settings→Pages 勾 Enforce HTTPS

---

## 十一、已完成（回顾）

- 首页：星空 + 星座网 + 粒子大字 Tiger Captain + 鼠标交互 + 导航栏 + 宠物老虎，视觉已定稿
- 全局导航栏：四入口 + 关于我下拉五项 + 滚动毛玻璃 + 移动端汉堡菜单
- 关于我页：五分区骨架 + scroll-spy + 领队分区（关于烛逢公众号/故事/我的小组/评价四入口）
- 工具箱页：三分类 22 张卡片，Wang Nan 开源工具署名齐全，外链新窗口
- 我的小组页：小程序（报名享折扣）+ 微信群（简介 + 折叠群公告）
- 评价页：4 条评价截图（统一尺寸）+ 真诚感谢文案
- 故事页 + 详情页：数据驱动，活动列表 + 详情两层结构，支持置顶
- 宠物老虎：精灵图逐帧（idle 4/run 8/sit 2/sleep 2/play 2）+ 点哪跑哪 + 方向翻转 + 点击互动 + 页面隐藏暂停，已按需求 5.1 全部实现
- 网站二维码：已生成（桌面 donghuaihao二维码.png，中心老虎，永久）

---

## 十二、合规红线（务必遵守）

1. **不放任何地图**；未来若做足迹/地图类页面，只能用有审图号的国内底图（天地图/高德/百度），禁止手绘中国国界线、禁止 Google Maps 等境外底图，中国地图必须完整（含南海诸岛、台湾等）
2. 引用 Wang Nan 的开源工具必须在卡片显著署名，遵守其仓库 License
3. 不放未经授权的第三方图片、字体、表情包素材（老虎形象是用户自己 AI 生成的，合规）
